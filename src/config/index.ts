export { envConfig } from '@/config/env';
export type { EnvConfig, RouterHistoryMode } from '@/config/env';
export { appSettings } from '@/config/settings';
export type { AppSettings, LayoutMode, MenuMode, PermissionMode, StorageType, ThemeMode } from '@/config/settings';
